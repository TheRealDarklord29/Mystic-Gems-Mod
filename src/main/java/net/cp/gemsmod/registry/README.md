# `registry` package guide

This package contains the canonical registration classes.

## Files
- `BlockRegistry`: registers blocks and related block items.
- `ItemRegistry`: registers items and creative tab entries.

## Notes
- Prefer using these classes from new code.
- Legacy wrappers (`ModBlocks`, `ModItems`) remain for compatibility.
