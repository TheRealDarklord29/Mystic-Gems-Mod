# `config` package guide

Runtime configuration loading and access.

## Files
- `ModConfig`: reads/writes `config/mysticgemsmod.json` and exposes typed accessors.

## Notes
- Add new balancing keys here.
- Keep defaults safe so the mod works even if config file is missing/invalid.
