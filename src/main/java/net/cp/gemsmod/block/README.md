# `block` package guide

Block classes and compatibility wrappers.

## Files
- `GemInfuserBlock`: custom Gem Infuser workbench behavior.
- `ModBlocks`: compatibility wrapper delegating to `BlockRegistry`.

## Notes
- Put block-specific behavior classes here.
- Keep registration in `registry/BlockRegistry`.
