# `item` package guide

Item behavior classes and compatibility wrappers.

## Files
- `GemItem`: interaction behavior for attunement and activation.
- `ModItems`: compatibility wrapper delegating to `ItemRegistry`.

## Notes
- Put item behavior implementation here.
- Keep item registration in `registry/ItemRegistry`.
